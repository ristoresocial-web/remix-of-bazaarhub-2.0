

# Logo Size Update

Update `src/components/BazaarLogo.tsx` to use responsive height classes: `h-8` on mobile, `md:h-10` on desktop.

Change the `<img>` className from `"h-8 w-auto object-contain"` to `"h-8 md:h-10 w-auto object-contain"`.

Single-file, single-line change.

